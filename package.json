{
  "name": "vite-reason-react",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite serve src",
    "build": "vite build src --outDir dist ",
    "re:build": "bsb -make-world -clean-world",
    "re:watch": "bsb -make-world -clean-world -w"
  },
  "dependencies": {
    "@pika/react": "^16.13.1",
    "@pika/react-dom": "^16.13.1"
  },
  "devDependencies": {
    "bs-platform": "^7.3.2",
    "reason-react": "^0.8.0",
    "vite": "^0.15.0"
  }
}
